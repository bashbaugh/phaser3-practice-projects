class Castle extends Phaser.Scene{

    constructor (config) {
        super(config);
    }
    
    preload () {
    }
    
    create (data)  {
      
    }
    update(time, delta) {super.update(time, delta);}

}

export default Castle;